// git diff --staged or --cached 

// ==> git diff --staged 
//        or 
// ==> git diff --cached 
// will list the chnages between the staging area and our last commit 

// "Show what will be included in ur commit if u run git commit right now"


